<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://flipped94.github.io</id>
    <title>Flipped&apos;s Blog</title>
    <updated>2021-03-25T17:19:56.938Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://flipped94.github.io"/>
    <link rel="self" href="https://flipped94.github.io/atom.xml"/>
    <subtitle>The higher I got, the more amazed I was by the view.</subtitle>
    <logo>https://flipped94.github.io/images/avatar.png</logo>
    <icon>https://flipped94.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, Flipped&apos;s Blog</rights>
    <entry>
        <title type="html"><![CDATA[win10+logstash导入数据坑]]></title>
        <id>https://flipped94.github.io/post/RbMQQqRZW/</id>
        <link href="https://flipped94.github.io/post/RbMQQqRZW/">
        </link>
        <updated>2021-03-25T17:14:54.000Z</updated>
        <content type="html"><![CDATA[<p>正确的如下:</p>
<pre><code class="language-json">input {
  file {
    path =&gt; &quot;D:/software/logstash-7.10.1/movies.csv&quot;
    start_position =&gt; &quot;beginning&quot;
    sincedb_path =&gt; &quot;NUL&quot;
  }
}
filter {
  csv {
    separator =&gt; &quot;,&quot;
    columns =&gt; [&quot;id&quot;,&quot;content&quot;,&quot;genre&quot;]
  }

  mutate {
    split =&gt; { &quot;genre&quot; =&gt; &quot;|&quot; }
    remove_field =&gt; [&quot;path&quot;, &quot;host&quot;,&quot;@timestamp&quot;,&quot;message&quot;]
  }

  mutate {

    split =&gt; [&quot;content&quot;, &quot;(&quot;]
    add_field =&gt; { &quot;title&quot; =&gt; &quot;%{[content][0]}&quot;}
    add_field =&gt; { &quot;year&quot; =&gt; &quot;%{[content][1]}&quot;}
  }

  mutate {
    convert =&gt; {
      &quot;year&quot; =&gt; &quot;integer&quot;
    }
    strip =&gt; [&quot;title&quot;]
    remove_field =&gt; [&quot;path&quot;, &quot;host&quot;,&quot;@timestamp&quot;,&quot;message&quot;,&quot;content&quot;]
  }

}
output {
   elasticsearch {
     hosts =&gt; &quot;http://localhost:9200&quot;
     index =&gt; &quot;movies&quot;
     document_id =&gt; &quot;%{id}&quot;
   }
  stdout {}
}
</code></pre>
<p>sincedb_path:可以是一个不存在的文件。<br>
path：windows下文件分隔如果&quot;&quot;，不会导入数据，也没有error信息，需要改成：&quot;/&quot;，奇怪的是sincedb_path文件分隔用&quot;&quot;。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Mybatis传递多个参数]]></title>
        <id>https://flipped94.github.io/post/fzhTdOqQh/</id>
        <link href="https://flipped94.github.io/post/fzhTdOqQh/">
        </link>
        <updated>2021-03-05T04:45:47.000Z</updated>
        <content type="html"><![CDATA[<p>使用Mybatis最大的特性就是sql需要自己写，而写sql就需要传递多个参数。下面总结了以下几种多参数传递的方法。MyBatis版本为3.5.6。</p>
<h2 id="方法1顺序传参">方法1：顺序传参</h2>
<pre><code class="language-java">public  User selectUserByIdRange(int start, int offset);
</code></pre>
<pre><code class="language-xml">&lt;select id=&quot;selectUserByIdRange&quot; resultTyoe=&quot;work.flipped.domain.User&quot;&gt;
    select * from imc_user where user_name = #{param1} and dept_id = #{param2}
    &lt;!--或者--&gt;
     select * from imc_user where user_id between #{arg0} and #{arg1}
     &lt;!--还有一些版本使用--&gt;
     select * from imc_user where user_id between #{0} and #{1}
&lt;/select&gt;
</code></pre>
<p><code>#{}</code>代表传入参数的顺序。这种方法不建议使用，sql层表达不直观，且一旦顺序调整容易出错。</p>
<h2 id="方法2param注解传参">方法2：@Param注解传参</h2>
<pre><code class="language-java">List&lt;User&gt; selectUserByIdRange(@Param(&quot;start&quot;) int start, @Param(&quot;offset&quot;) int offset);
</code></pre>
<pre><code class="language-xml">&lt;select id=&quot;selectUserByIdRange&quot; resultTyoe=&quot;work.flipped.domain.User&quot;&gt;
     select * from imc_user where user_id between #{start} and #{offset}
&lt;/select&gt;
</code></pre>
<p><code>#{}</code> 里面的名称对应的是注解<code>@Param</code> 括号里面修饰的名称。这种方法在参数不多的情况比较直观的，推荐使用。</p>
<h2 id="方法3map传参">方法3：Map传参</h2>
<pre><code class="language-java">List&lt;User&gt; selectUserByIdRange(Map&lt;String, Object&gt; map);
</code></pre>
<pre><code class="language-xml">&lt;select id=&quot;selectUserByIdRange&quot; parameterType=&quot;java.util.Map&quot; resultType=&quot;work.flipped.domain.User&quot;&gt;
        select * from imc_user where user_id between #{start} and #{offset}
    &lt;/select&gt;
</code></pre>
<p><code>#{}</code>里面的名称对应的是 Map里面的key名称。这种方法适合传递多个参数，且参数易变能灵活传递的情况。</p>
<h2 id="方法4java-bean传参">方法4：Java Bean传参</h2>
<pre><code class="language-java">@Data
@Builder
public class Range {
    private int start;
    private int offset;
}
</code></pre>
<pre><code class="language-java">List&lt;User&gt; selectUserByIdRange(Range range);
</code></pre>
<pre><code class="language-xml">&lt;select id=&quot;selectUserByIdRange&quot; parameterType=&quot;work.flipped.domain.dto.Range&quot; resultType=&quot;work.flipped.domain.User&quot;&gt;
    select * from imc_user where user_id between #{start} and #{offset}
&lt;/select&gt;
</code></pre>
<p><code>#{}</code> 里面的名称对应的是 Range 类里面的成员属性。这种方法很直观，但需要建一个实体类，扩展不容易，需要加属性（结合Lombok很好用），看情况使用。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[RocketMQ控制台安装教程]]></title>
        <id>https://flipped94.github.io/post/2hNfLJsXh/</id>
        <link href="https://flipped94.github.io/post/2hNfLJsXh/">
        </link>
        <updated>2021-02-21T02:08:33.000Z</updated>
        <content type="html"><![CDATA[<p>本文详细探讨如何搭建RocketMQ控制台，RocketMQ的可视化管理界面。</p>
<h2 id="一-下载代码">一、下载代码</h2>
<p>方式一、git下载，执行如下命令<br>
git clone https://github.com/apache/rocketmq-externals.git</p>
<p>方式二、直接下载，访问如下地址即可<br>
https://github.com/apache/rocketmq-externals/archive/master.zip</p>
<h2 id="二-修改控制台代码">二、修改控制台代码</h2>
<h3 id="21-修改配置">2.1 修改配置</h3>
<p>找到 <code>rocketmq-console/src/main/resources/application.properties</code> 根据需求，修改配置。笔者只修改了如下两项。</p>
<h5 id="控制台的端口">控制台的端口</h5>
<p><code>server.port=9020</code></p>
<h4 id="namesrv_addr-地址">NAMESRV_ADDR 地址</h4>
<p>也可以不修改，在启动完console后，在控制台导航栏 - 运维 - NameSvrAddrList一栏设置<br>
<code>rocketmq.config.namesrvAddr=localhost:9876</code></p>
<h3 id="22-修改依赖">2.2 修改依赖</h3>
<p>修改 <code>pom.xml</code> ，修改 RocketMQ 相关依赖的版本</p>
<p>找到 <code>&lt;rocketmq.version&gt;4.8.0&lt;/rocketmq.version&gt;</code>修改为<code>&lt;rocketmq.version&gt;你的RocketMQ版本&lt;/rocketmq.version&gt;</code>。因为笔者使用的 Rocket MQ 的版本是4.8.0，所以不需要修改。</p>
<h4 id="23-修改代码">2.3 修改代码</h4>
<p>修改pom.xml后，<code>org.apache.rocketmq.console.service.impl.MessageServiceImpl#queryMessageByTopic</code> 编译会报错，需要将</p>
<pre><code class="language-java">DefaultMQPullConsumer consumer = new DefaultMQPullConsumer(MixAll.TOOLS_CONSUMER_GROUP, null)；
</code></pre>
<p>改为：</p>
<pre><code class="language-java">RPCHook rpcHook = null;
DefaultMQPullConsumer consumer = new DefaultMQPullConsumer(MixAll.TOOLS_CONSUMER_GROUP, rpcHook);
</code></pre>
<p>即可。</p>
<h2 id="三-打包构建">三、打包构建</h2>
<p>切换到目录<code>rocketmq-console</code>，执行下面的命令</p>
<pre><code class="language-bash">mvn clean package -DskipTests
</code></pre>
<h2 id="四-启动">四、启动</h2>
<pre><code class="language-bash">java -jar .\target\rocketmq-console-ng-2.0.0.jar
</code></pre>
<h2 id="五-访问">五、访问</h2>
<p>访问 http://localhost:9020 (端口用上面 application.properties 中的 server.port=9020指定，默认是8080) ，即可看到类似如下的界面：<br>
<img src="https://img.flipped.work/20210221104517.png"/></p>
<h1 id="六-控制台使用说明">六、控制台使用说明</h1>
<p><a href="https://github.com/eacdy/rocketmq-externals/blob/master/rocketmq-console/doc/1_0_0/UserGuide_CN.md">RocketMQ控制台使用文档</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Windows 10下 安装 RocketMQ]]></title>
        <id>https://flipped94.github.io/post/maPBAxTLT/</id>
        <link href="https://flipped94.github.io/post/maPBAxTLT/">
        </link>
        <updated>2021-02-20T13:16:52.000Z</updated>
        <content type="html"><![CDATA[<p>本文详细探讨如何搭建RocketMQ</p>
<h2 id="一-下载">一、下载</h2>
<p>前往https://www.apache.org/dyn/closer.cgi?path=rocketmq/4.8.0/rocketmq-all-4.8.0-bin-release.zip ，下载 Binary 文件即可。</p>
<h2 id="二-预备环境">二、预备环境</h2>
<ul>
<li>Windows 10 64位操作系统</li>
<li>64 位 JDK 1.8</li>
<li>4G+的可用磁盘</li>
</ul>
<h3 id="三-配置">三、配置</h3>
<p>配置两个系统环境变量配置<br>
变量名： ROCKETMQ_HOME<br>
变量值：&quot;D:\rocketmq&quot;<br>
变量名： NAMESRV_ADDR<br>
变量值：&quot;localhost:9876&quot;<br>
<img src="https://img.flipped.work/20210220220011.png"/><br>
<img src="https://img.flipped.work/20210220220047.png"/><br>
如果使用 PowerShell，也可以如下面这样输入环境变量（如果关闭了PowerShell，需要重新输入）：</p>
<pre><code class="language-bash">$Env:ROCKETMQ_HOME=&quot;D:\rocketmq&quot;
$Env:NAMESRV_ADDR=&quot;localhost:9876&quot;
</code></pre>
<h2 id="四-启动">四、启动</h2>
<p>如果会出现内存不足的情况，需要修改找到 bin 目录下 runserver.sh和runbroker.sh，编辑<br>
<code>set &quot;JAVA_OPT=%JAVA_OPT% -server -Xms2g -Xmx2g -Xmn1g -XX:MetaspaceSize=128m -XX:MaxMetaspaceSize=320m&quot;</code>行，调整为合适的内存。</p>
<h3 id="启动nameserver">启动NAMESERVER</h3>
<p>打开一个 PowerShell 窗口，切换目录到RocketMQ根目录，然后执行 <code>.\bin\mqnamesrv.cmd</code>，启动 NAME SERVER。此窗口不要关闭。</p>
<h3 id="启动-broker">启动 Broker</h3>
<p>打开第二个 PowerShell 窗口，切换目录到RocketMQ根目录，然后执行 <code>.\bin\mqbroker.cmd -n localhost:9876 autoCreateTopicEnable=true</code>，启动 BROKER。此窗口不要关闭。</p>
<h2 id="五-验证rocketmq功能正常">五、验证RocketMQ功能正常</h2>
<h3 id="验证生产消息正常">验证生产消息正常</h3>
<p>打开第三个 PowerShell 窗口，切换目录到RocketMQ根目录，执行如下命令：</p>
<pre><code class="language-bash">.\bin\tools.cmd  org.apache.rocketmq.example.quickstart.Producer
</code></pre>
<p>看到类似输出：</p>
<pre><code class="language-bash">SendResult [sendStatus=SEND_OK, msgId=7F0000011C683764951D669C6BC10000, offsetMsgId=C0A8011E00002A9F0000000000000000, messageQueue=MessageQueue [topic=TopicTest, brokerName=DESKTOP-K5RATJ5, queueId=3], queueOffset=0]
</code></pre>
<h3 id="验证消费消息正常">验证消费消息正常</h3>
<p>打开第四个 PowerShell 窗口，切换目录到RocketMQ根目录，执行如下命令：</p>
<pre><code class="language-bash">.\bin\tools.cmd  org.apache.rocketmq.example.quickstart.Consumer
</code></pre>
<p>看到类似输出：</p>
<pre><code class="language-bash">ConsumeMessageThread_1 Receive New Messages: [MessageExt [brokerName=DESKTOP-K5RATJ5, queueId=2, storeSize=203, queueOffset=236, sysFlag=0, bornTimestamp=1613830330877, bornHost=/192.168.1.30:4099, storeTimestamp=1613830330877, storeHost=/192.168.1.30:10911, msgId=C0A8011E00002A9F000000000002EE83, commitLogOffset=192131, bodyCRC=1948249169, reconsumeTimes=0, preparedTransactionOffset=0, toString()=Message{topic='TopicTest', flag=0, properties={MIN_OFFSET=0, MAX_OFFSET=250, CONSUME_START_TIME=1613830877782, UNIQ_KEY=7F0000011C683764951D669C79FD03B3, CLUSTER=DefaultCluster, WAIT=true, TAGS=TagA}, body=[72, 101, 108, 108, 111, 32, 82, 111, 99, 107, 101, 116, 77, 81, 32, 57, 52, 55], transactionId='null'}]]
</code></pre>
<h2 id="六-关闭">六、关闭</h2>
<p>一般可以直接关闭所有的 PowerShell 窗口就行，生产环境别这样做就好。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[通用 Mapper 专用代码生成器把其他数据库的同名表生成下来的问题]]></title>
        <id>https://flipped94.github.io/post/ZlEmf6-U8/</id>
        <link href="https://flipped94.github.io/post/ZlEmf6-U8/">
        </link>
        <updated>2021-02-18T09:15:38.000Z</updated>
        <content type="html"><![CDATA[<pre><code class="language-yaml">Table Configuration user matched more than one table (user_center..user,mysql..user)
Column id, specified as a generated key column in table user, does not exist in the table.
</code></pre>
<p>在使用生成器生成代码的时候遇到了这个错误，现象就是某个类中出来了数据库表里面没有的字段，解决方法是在生成器的配置文件里的数据库连接地址(就是jdbcUrl)中添加这个参数：</p>
<pre><code class="language-yml">nullCatalogMeansCurrent=true
</code></pre>
<p>完整的 generatorConfig.xml 配置文件如下：</p>
<pre><code class="language-xml">&lt;!DOCTYPE generatorConfiguration
        PUBLIC &quot;-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN&quot;
        &quot;http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd&quot;&gt;

&lt;generatorConfiguration&gt;
    &lt;properties resource=&quot;generator/config.properties&quot;/&gt;

    &lt;context id=&quot;Mysql&quot; targetRuntime=&quot;MyBatis3Simple&quot; defaultModelType=&quot;flat&quot;&gt;
        &lt;property name=&quot;beginningDelimiter&quot; value=&quot;`&quot;/&gt;
        &lt;property name=&quot;endingDelimiter&quot; value=&quot;`&quot;/&gt;

        &lt;plugin type=&quot;tk.mybatis.mapper.generator.MapperPlugin&quot;&gt;
            &lt;property name=&quot;mappers&quot; value=&quot;tk.mybatis.mapper.common.Mapper&quot;/&gt;
            &lt;property name=&quot;caseSensitive&quot; value=&quot;true&quot;/&gt;
        &lt;/plugin&gt;

        &lt;jdbcConnection driverClass=&quot;${jdbc.driverClass}&quot;
                        connectionURL=&quot;${jdbc.url}&quot;
                        userId=&quot;${jdbc.user}&quot;
                        password=&quot;${jdbc.password}&quot;&gt;
        &lt;/jdbcConnection&gt;

        &lt;!--实体--&gt;
        &lt;javaModelGenerator targetPackage=&quot;work.flipped.usercenter.domain.entity.${moduleName}&quot;
                            targetProject=&quot;src/main/java&quot;/&gt;

        &lt;!--mapper.xml--&gt;
        &lt;sqlMapGenerator targetPackage=&quot;work.flipped.usercenter.dao.${moduleName}&quot;
                         targetProject=&quot;src/main/resources&quot;/&gt;

        &lt;!--mapper接口--&gt;
        &lt;javaClientGenerator targetPackage=&quot;work.flipped.usercenter.dao.${moduleName}&quot;
                             targetProject=&quot;src/main/java&quot;
                             type=&quot;XMLMAPPER&quot;/&gt;

        &lt;!--为哪张表生成代码--&gt;
        &lt;table tableName=&quot;${tableName}&quot;&gt;
            &lt;generatedKey column=&quot;id&quot; sqlStatement=&quot;JDBC&quot;/&gt;
        &lt;/table&gt;
    &lt;/context&gt;
&lt;/generatorConfiguration&gt;
</code></pre>
<p>config.properties 文件如下：</p>
<pre><code class="language-yml">jdbc.url=jdbc:mysql://localhost:3306/user_center?serverTimezone=Asia/Shanghai&amp;nullCatalogMeansCurrent=true
jdbc.driverClass=com.mysql.cj.jdbc.Driver
jdbc.user=root
jdbc.password=flipped
# 模块名称
moduleName=user
# 表名
tableName=user
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Kafka（三）Producer]]></title>
        <id>https://flipped94.github.io/post/UqOG-r8J9/</id>
        <link href="https://flipped94.github.io/post/UqOG-r8J9/">
        </link>
        <updated>2021-01-24T07:41:54.000Z</updated>
        <content type="html"><![CDATA[<p>本文主要关于 Kafka Producer 设计和 Producer API。</p>
<h2 id="producer-设计">Producer 设计</h2>
<h3 id="负载均衡">负载均衡</h3>
<p>Producer 直接将数据发送给作为分区领导者的代理，而不需要任何介入的路由层。为了帮助 Producer 做到这一点，所有的 Kafka 节点可以在任何时间，响应关于哪些服务器处于活动状态以及主题的分区的领导者在哪里的元数据请求，以便 Producer 适当地发送请求。</p>
<p>客户端控制将消息发布到哪个分区，这可以是随机完成的（随机负载均衡），也可以通过某些语义分区函数完成。Kafka 提供语义分区的接口，允许用户指定要分区的键，并使用该键对分区进行散列(如果需要，还可以覆盖分区函数)。</p>
<h3 id="异步发送">异步发送</h3>
<p>批处理是效率的主要驱动之一，为了实现批处理，Producer 在内存中积累数据，并在单个请求批量中发送。可以将批处理配置为累积不超过固定数量的消息，等待时间不超过某个固定延迟范围(比如64k或10毫秒)。这样就可以累积更多的字节来发送，并减少服务器的 I/O 操作。这个缓冲区是可配置的，它提供了一种机制，可以用少量额外的延迟来换取更好的吞吐量。</p>
<h2 id="producer-api">Producer API</h2>
<p>Producer API 允许应用程序向Kafka集群中的主题发送数据流。需要以下 Maven 依赖：</p>
<pre><code class="language-xml">&lt;dependency&gt;
    &lt;groupId&gt;org.apache.kafka&lt;/groupId&gt;
    &lt;artifactId&gt;kafka-clients&lt;/artifactId&gt;
    &lt;version&gt;2.7.0&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>Kafka 客户端，将记录发布到 Kafka 集群。 Producer 是线程安全的，并且跨线程共享单个 Producer 实例通常比拥有多个实例要快。<br>
下面是一个简单的示例，发送连续数字的字符串作为键/值对的记录。</p>
<pre><code class="language-java">public static void producerSend() {
        Properties props = new Properties();
        props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, &quot;localhost:9092&quot;);
        props.put(ProducerConfig.ACKS_CONFIG, &quot;all&quot;);
        props.put(ProducerConfig.RETRIES_CONFIG, &quot;0&quot;);
        props.put(ProducerConfig.BATCH_SIZE_CONFIG, &quot;16384&quot;);
        props.put(ProducerConfig.LINGER_MS_CONFIG, &quot;1&quot;);
        props.put(ProducerConfig.BUFFER_MEMORY_CONFIG, &quot;33554432&quot;);
        props.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, &quot;org.apache.kafka.common.serialization.StringSerializer&quot;);
        props.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, &quot;org.apache.kafka.common.serialization.StringSerializer&quot;);
        // Producer的主对象
        Producer&lt;String, String&gt; producer = new KafkaProducer&lt;&gt;(props);

        for (int i = 0; i &lt; 100; i++) {
            // ProducerRecord：消息对象
            producer.send(new ProducerRecord&lt;String, String&gt;(&quot;test-topic&quot;, &quot;key-&quot; + i, &quot;value-&quot; + i));
        }
        // 所有的通道打开都需要关闭
        producer.close();
}
</code></pre>
<p>Producer 由一个缓冲区和一个后台 I/O 线程组成，前者保存尚未传输到服务器的记录，后者负责将这些记录转换为请求并传输到集群。如果在使用之后没有关闭 Producer，这些资源就会泄漏。</p>
<p><code>send()</code> 方法是异步的。调用时，它将记录添加到等待记录发送的缓冲区中并立即返回。这允许 Producer 批量处理记录以提高效率。</p>
<p><code>acks</code> 配置控制请求完成的条件。设置为 <code>all</code> 时将导致阻塞在提交记录，这是最慢但最持久的设置。</p>
<p>如果请求失败，Producer 可以自动重试，但由于将 <code>retries</code> 指定为0，所以它不会重试。启用重试还增加了重复的可能性。</p>
<p>Producer 为每个分区维护未发送记录的缓冲区。这些缓冲区的大小由 <code>batch.size</code> 配置指定。增大这个值可能会导致更多的批处理，但需要更多的内存(因为通常为每个活动分区设置一个这样的缓冲区)。</p>
<p>默认情况下，即使缓冲区中有额外的未使用空间，也可以立即发送缓冲区。如果想减少请求的数量，可以设置 <code>linger.ms</code> 的值大于0，表示 Producer 在发送请求之前等待的毫秒数，希望有更多的记录填充缓冲区。例如，上面的代码片段中，因为将 <code>linger.ms</code> 设置为1毫秒，所以可能所有100条记录都将在一个请求中发送，。但是，如果没有填满缓冲区，这个设置会给请求增加1毫秒的延迟。到达缓冲区时间接近的记录通常会在一个批处理，即使 <code>linger.ms</code> 的值为0，因此，在大负载下，会忽视 <code>linger.ms</code> 配置；将此配置设置为大于0的值可以在不处于最大负载下时导致更少、更有效的请求，代价是少量的延迟。</p>
<p><code>buffer.memory</code> 控制 Producer 可用来缓冲的内存总量。如果记录的发送速度比它们传输到服务器的速度快，那么这个缓冲区空间将被耗尽。当缓冲区空间耗尽时，将阻塞其他的 <code>send</code> 调用。阻塞时间的阈值由 <code>max.block.ms</code> 决定，如果超市会抛出 <code>TimeoutException</code> 异常。</p>
<p><code>key.serializer</code> 和 <code>value.serializer</code> 表示如何将 <code>ProducerRecord</code> 提供的键和值对象转换为字节。对于简单的字符串或字节类型，可以使用内置的 <code>ByteArraySerializer</code> 或 <code>StringSerializer</code>。</p>
<p>从Kafka 0.11开始，KafkaProducer 支持另外两种模式：幂等 Producer 和 事务 Producer。幂等 Producer 增强了 Kafka 的交付语义，从至少一次交付（at least once）到精确一次交付（exactly once）。特别是 Producer 重试将不再引入重复。事务 Producer 允许应用程序自动地将消息发送到多个分区(和主题!)。</p>
<p><code>enable.idempotence</code> 设置为 <code>true</code> 开启等幂。开启后，<code>retries</code> 配置将默认为<code>Integer.MAX_VALUE</code> ，<code>acks</code> 配置将默认为 <code>all</code>。幂等 Producer 不需要修改API，因此不需要修改现有的应用程序来利用这个特性。</p>
<p>为了利用幂等 Producer，必须避免应用程序级的重发，因为这些不能重复。因此，如果应用程序启用幂等性，建议不设置重试配置，因为它将默认为 <code>Integer.MAX_VALUE</code>。此外，如果 <code>send(ProducerRecord)</code> 在无限重试的情况下返回一个错误(例如，消息在发送之前在缓冲区中过期)，那么建议关闭 Producer 并检查最后生成的消息的内容，确保它没有重复。最后，Producer 只能保证在单个会话中发送的消息的幂等性。</p>
<p>要使用事务 Producer，必须设置 <code>transactional.id</code>。设置后，幂等性就会自动与幂等性所依赖的 Producer 配置一起启用。此外，事务中包含的主题应该配置持久性。特别是 <code>replication.factor</code> 应该至少为3，这些主题的 <code>min.insync.replicas</code> 应该设置为2。最后，为了从端到端实现事务保证，Consumer 也必须被配置为只读取已提交的消息。</p>
<p><code>transactional.id</code> 的目的是在单个 Producer 实例的多个会话之间启用事务恢复。它通常是从分区的、有状态的应用程序中的shard标识符派生的。因此，对于在分区应用程序中运行的每个生产者实例，它应该是唯一的。</p>
<p>所有新的事务 api 都是阻塞的，并在失败时抛出异常。下面的示例说明了如何使用新api。它与上面的示例类似，只是所有100条消息都是一个事务的一部分。</p>
<pre><code class="language-java">Properties props = new Properties();
 props.put(&quot;bootstrap.servers&quot;, &quot;localhost:9092&quot;);
 props.put(&quot;transactional.id&quot;, &quot;my-transactional-id&quot;);
 Producer&lt;String, String&gt; producer = new KafkaProducer&lt;&gt;(props, new StringSerializer(), new StringSerializer());

 producer.initTransactions();

 try {
     producer.beginTransaction();
     for (int i = 0; i &lt; 100; i++)
         producer.send(new ProducerRecord&lt;&gt;(&quot;my-topic&quot;, Integer.toString(i), Integer.toString(i)));
     producer.commitTransaction();
 } catch (ProducerFencedException | OutOfOrderSequenceException | AuthorizationException e) {
     // We can't recover from these exceptions, so our only option is to close the producer and exit.
     producer.close();
 } catch (KafkaException e) {
     // For all other exceptions, just abort the transaction and try again.
     producer.abortTransaction();
 }
 producer.close();
</code></pre>
<p>正如示例中所示，每个 Producer 只能有一个打开的事务。在 <code>beginTransaction()</code> 和 <code>commitTransaction()</code> 调用之间发送的所有消息都将是单个事务的一部分。设置了 <code>transactional.id</code> 时，Producer 发送的所有消息必须是事务的一部分。</p>
<p>事务 Producer 使用异常传达错误状态。特别是，不需要为 <code>producer.send()</code> 设置回调或 在返回的 <code>Future</code> 上调用 <code>.get()</code>：如果任何 <code>producer.send()</code> 或事务调用在事务期间出现不可恢复的错误，将引发 KafkaException。</p>
<p>通过在收到 KafkaException 时调用 <code>producer.send()</code>，可以确保任何成功的写入都标记为已中止，从而保留事务性保证。</p>
<p>此客户端可以与版本 0.10.0 或更高版本的代理进行通信。较旧或较新的代理可能不支持某些客户端功能。例如，事务 API 需要代理版本 0.11.0 或更新版本。调用正在运行的代理版本中不可用的 API 时，将收到不受支持的返回异常。<br>
除了第一个示例中的异步发送模式外，还有同步发送（异步阻塞）和异步回调两种模式。<br>
<strong>同步发送（异步阻塞）</strong><br>
同步发送与异步发送的区别在于发送后的操作，由于每次发送后都调用了 <code>Future#get</code>，相当于阻塞在该处，代码如下所示。</p>
<pre><code class="language-java">/**
     * 同步发送（异步阻塞）
     * 
     * @throws ExecutionException
     * @throws InterruptedException
*/
public static void producerSyncSend() throws InterruptedException, ExecutionException {
        Properties props = new Properties();
        props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, &quot;localhost:9092&quot;);
        props.put(ProducerConfig.ACKS_CONFIG, &quot;all&quot;);
        props.put(ProducerConfig.RETRIES_CONFIG, &quot;0&quot;);
        props.put(ProducerConfig.BATCH_SIZE_CONFIG, &quot;16384&quot;);
        props.put(ProducerConfig.LINGER_MS_CONFIG, &quot;1&quot;);
        props.put(ProducerConfig.BUFFER_MEMORY_CONFIG, &quot;33554432&quot;);
        props.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, &quot;org.apache.kafka.common.serialization.StringSerializer&quot;);
        props.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG,
                &quot;org.apache.kafka.common.serialization.StringSerializer&quot;);
        // Producer的主对象
        Producer&lt;String, String&gt; producer = new KafkaProducer&lt;&gt;(props);

        for (int i = 0; i &lt; 100; i++) {
            // ProducerRecord：消息对象
            Future&lt;RecordMetadata&gt; future = producer
                    .send(new ProducerRecord&lt;String, String&gt;(&quot;test-topic&quot;, &quot;key-&quot; + i, &quot;value-&quot; + i));
            RecordMetadata recordMetadata = future.get();
            System.out.println(&quot;partition : &quot; + recordMetadata.partition() + &quot;, offset : &quot; + recordMetadata.offset());
        }
        // 所有的通道打开都需要关闭
        producer.close();
}
</code></pre>
<p><strong>异步回调发送</strong></p>
<pre><code class="language-java"> /**
     * 异步发送带回调
     */
    public static void producerSendWithCallback() {
        Properties props = new Properties();
        props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, &quot;localhost:9092&quot;);
        props.put(ProducerConfig.ACKS_CONFIG, &quot;all&quot;);
        props.put(ProducerConfig.RETRIES_CONFIG, &quot;0&quot;);
        props.put(ProducerConfig.BATCH_SIZE_CONFIG, &quot;16384&quot;);
        props.put(ProducerConfig.LINGER_MS_CONFIG, &quot;1&quot;);
        props.put(ProducerConfig.BUFFER_MEMORY_CONFIG, &quot;33554432&quot;);
        props.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, &quot;org.apache.kafka.common.serialization.StringSerializer&quot;);
        props.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG,
                &quot;org.apache.kafka.common.serialization.StringSerializer&quot;);
        // Producer的主对象
        Producer&lt;String, String&gt; producer = new KafkaProducer&lt;&gt;(props);

        for (int i = 0; i &lt; 100; i++) {
            // ProducerRecord：消息对象
            producer.send(new ProducerRecord&lt;String, String&gt;(&quot;test-topic&quot;, &quot;key-&quot; + i, &quot;value-&quot; + i), new Callback() {
                @Override
                public void onCompletion(RecordMetadata recordMetadata, Exception e) {
                    System.out.println(
                            &quot;partition : &quot; + recordMetadata.partition() + &quot;, offset : &quot; + recordMetadata.offset());
                };
            });
        }
        // 所有的通道打开都需要关闭
        producer.close();
}
</code></pre>
<p>前面的示例中，每个 Partition 的记录是不确定。载均衡的目的是将消息尽可能平均分配，尽可能将消息平均分配给所有分区<br>
<strong>自定义 Partition 负载均衡</strong><br>
自定义 Partition， 实现 <code>Partitioner</code> 接口，自己实现分区方法。<br>
在创建 <code>test-topic</code> 时，指定了3个分区，这里简单的对 key 做处理之后，取余。</p>
<pre><code class="language-java">// 自定义 Partition
public class SamplePartition implements Partitioner {

    @Override
    public void configure(Map&lt;String, ?&gt; arg0) {
        // TODO Auto-generated method stub

    }

    @Override
    public void close() {
        // TODO Auto-generated method stub

    }

    @Override
    public int partition(String topic, Object key, byte[] keyBytes, Object Value, byte[] valueBytes, Cluster cluster) {
        String keyStr = key + &quot;&quot;;
        String keyInt = keyStr.substring(4);
        System.out.println(&quot;======== keyInt ==========&quot; + keyInt);
        int i = Integer.parseInt(keyInt);
        System.out.println(&quot;======== i ==========&quot; + i);

        return i % 3;
    }
}
</code></pre>
<pre><code class="language-java">/**
     * 使用自定义 Partition
     * 异步发送带回调和Partition
*/
public static void producerSendWithCallbackAndPartition() {
        Properties props = new Properties();
        props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, &quot;localhost:9092&quot;);
        props.put(ProducerConfig.ACKS_CONFIG, &quot;all&quot;);
        props.put(ProducerConfig.RETRIES_CONFIG, &quot;0&quot;);
        props.put(ProducerConfig.BATCH_SIZE_CONFIG, &quot;16384&quot;);
        props.put(ProducerConfig.LINGER_MS_CONFIG, &quot;1&quot;);
        props.put(ProducerConfig.BUFFER_MEMORY_CONFIG, &quot;33554432&quot;);
        props.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, &quot;org.apache.kafka.common.serialization.StringSerializer&quot;);
        props.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG,
                &quot;org.apache.kafka.common.serialization.StringSerializer&quot;);
        props.put(ProducerConfig.PARTITIONER_CLASS_CONFIG, &quot;work.flipped.kafka.producer.SamplePartition&quot;);
        // Producer的主对象
        Producer&lt;String, String&gt; producer = new KafkaProducer&lt;&gt;(props);

        for (int i = 0; i &lt; 100; i++) {
            // ProducerRecord：消息对象
            producer.send(new ProducerRecord&lt;String, String&gt;(&quot;test-topic&quot;, &quot;key-&quot; + i, &quot;value-&quot; + i), new Callback() {
                @Override
                public void onCompletion(RecordMetadata recordMetadata, Exception e) {
                    System.out.println(
                            &quot;partition : &quot; + recordMetadata.partition() + &quot;, offset : &quot; + recordMetadata.offset());
                };
            });
        }
        // 所有的通道打开都需要关闭
        producer.close();
}
</code></pre>
<p>三个 Partition 的记录几乎一样多，分别为33，33，34，运行结果如图所示。<img src="https://img.flipped.work/img/20210124212456.png"/></p>
<h2 id="消息传递保障">消息传递保障</h2>
<p>在下一章介绍 Kafka Consumer后，介绍此内容。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Kafka（二）Admin API]]></title>
        <id>https://flipped94.github.io/post/qqGWSVvBP/</id>
        <link href="https://flipped94.github.io/post/qqGWSVvBP/">
        </link>
        <updated>2021-01-24T02:19:41.000Z</updated>
        <content type="html"><![CDATA[<p>Kafka 包括五个核心 API：</p>
<ul>
<li>Admin API 允许管理和检查主题、代理和其他Kafka对象。</li>
<li>Producer  API 允许应用程序向 Kafka 集群中的主题发送数据流。</li>
<li>Consumer API 允许应用程序从 Kafka 集群中的主题中读取数据流。</li>
<li>Streams API 允许将数据流从输入主题转换为输出主题。</li>
<li>Connect API 允许实现连续地从一些源系统或应用程序拉入Kafka 或从 Kafka 推入一些接收器系统或应用程序的连接器。</li>
</ul>
<p>Kafka 在一个独立于语言的协议上公开了它所有的功能，该协议有许多编程语言的客户端可用。然而，只有Java 客户端作为 Kafka 主要项目的一部分进行维护，其他的都可以作为独立的开源项目使用。<br>
本文只介绍 Admin API，其他 API 在后续章节给出。</p>
<h2 id="admin-api">Admin API</h2>
<p>Admin API支持管理和检查 Topic、Boker、ACL 和其他 Kafka 对象。要使用 Admin API，需要添加以下Maven依赖项:</p>
<pre><code class="language-xml">&lt;dependency&gt;
    &lt;groupId&gt;org.apache.kafka&lt;/groupId&gt;
    &lt;artifactId&gt;kafka-clients&lt;/artifactId&gt;
    &lt;version&gt;2.7.0&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>有关 Admin API 的更多信息，请参阅<a href="https://kafka.apache.org/27/javadoc/index.html?org/apache/kafka/clients/admin/Admin.html">javadoc</a>。<br>
相对比较重要的几个 API，如下表所示。</p>
<table>
<thead>
<tr>
<th>API</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>AdminClient</td>
<td>AdminClient 对象</td>
</tr>
<tr>
<td>NewTopic</td>
<td>创建 Topic</td>
</tr>
<tr>
<td>CreateTopicsResult</td>
<td>创建 Topic 的返回结果</td>
</tr>
<tr>
<td>ListTopicsResult</td>
<td>查询 Topic 列表</td>
</tr>
<tr>
<td>ListTopicsOptions</td>
<td>查询 Topic 列表及选项</td>
</tr>
<tr>
<td>DescribeTopicsResult</td>
<td>查询 Topics</td>
</tr>
<tr>
<td>DescribeConfigsResult</td>
<td>查询 Topics 配置项</td>
</tr>
</tbody>
</table>
<h3 id="创建-adminclient">创建 AdminClient</h3>
<pre><code class="language-java">public static final String TOPIC_NAME = &quot;test-topic&quot;;

    public static AdminClient getAdminClient() {
        Properties properties = new Properties();
        properties.setProperty(AdminClientConfig.BOOTSTRAP_SERVERS_CONFIG, &quot;localhost:9092&quot;);
        return AdminClient.create(properties);
}
</code></pre>
<h3 id="创建-topic">创建 Topic</h3>
<pre><code class="language-java">public static void createTopic() {
        AdminClient adminClient = getAdminClient();
        short rs = 1; // 副本因子
        NewTopic newTopic = new NewTopic(TOPIC_NAME, 3, rs);
        CreateTopicsResult topics = adminClient.createTopics(Arrays.asList(newTopic));
        System.out.println(&quot;topics: &quot; + topics);
}
</code></pre>
<h3 id="查看-topic-列表">查看 Topic 列表</h3>
<pre><code class="language-java">public static void topicList() {
        AdminClient adminClient = getAdminClient();
        try {
            ListTopicsResult listTopicsResult = adminClient.listTopics();
            Set&lt;String&gt; names = listTopicsResult.names().get();
            System.out.println(&quot;================&quot;);
            names.forEach(System.out::println);
            System.out.println(&quot;================&quot;);
        } catch (InterruptedException | ExecutionException e) {
            e.printStackTrace();
        }
}
</code></pre>
<h3 id="删除-topic">删除 Topic</h3>
<pre><code class="language-java">public static void deleteTopic() {
        AdminClient adminClient = getAdminClient();
        DeleteTopicsResult deleteTopicsResult = adminClient.deleteTopics(Collections.singletonList(TOPIC_NAME));
        try {
            deleteTopicsResult.all().get();
        } catch (InterruptedException | ExecutionException e) {
            e.printStackTrace();
        }
}
</code></pre>
<h3 id="描述-topic">描述 Topic</h3>
<pre><code class="language-java">public static void describeTopic() {
        AdminClient adminClient = getAdminClient();
        DescribeTopicsResult describeTopicsResult = adminClient
                .describeTopics(Arrays.asList(&quot;test-topic&quot;, &quot;second-topic&quot;));
        try {
            Map&lt;String, TopicDescription&gt; stringTopicDescriptionMap = describeTopicsResult.all().get();
            Set&lt;Map.Entry&lt;String, TopicDescription&gt;&gt; entries = stringTopicDescriptionMap.entrySet();
            System.out.println(&quot;===========&quot;);
            entries.forEach(entry -&gt; {
                System.out.println(entry.getKey() + &quot;:&quot; + entry.getValue());
                System.out.println(&quot;===========&quot;);
            });
        } catch (InterruptedException | ExecutionException e) {
            e.printStackTrace();
        }
}
</code></pre>
<h3 id="topic-配置信息">Topic 配置信息</h3>
<pre><code class="language-java">public static void describeConfig() {
        AdminClient adminClient = getAdminClient();
        ConfigResource configResource = new ConfigResource(ConfigResource.Type.TOPIC, TOPIC_NAME);
        DescribeConfigsResult describeConfigsResult = adminClient
                .describeConfigs(Collections.singletonList(configResource));
        Map&lt;ConfigResource, Config&gt; configResourceConfigMap = null;
        try {
            configResourceConfigMap = describeConfigsResult.all().get();
        } catch (InterruptedException | ExecutionException e) {
            e.printStackTrace();
        }
        assert configResourceConfigMap != null;
        Set&lt;Map.Entry&lt;ConfigResource, Config&gt;&gt; entries = configResourceConfigMap.entrySet();
        entries.forEach(entry -&gt; System.out.println(entry.getKey() + &quot;:&quot; + entry.getValue()));
}
</code></pre>
<h3 id="修改-topic-配置">修改 Topic 配置</h3>
<pre><code class="language-java">public static void alterConfigResult() {
        AdminClient adminClient = getAdminClient();
        Map&lt;ConfigResource, Config&gt; configMap = new HashMap&lt;&gt;();
        
        // 两个参数
        ConfigResource configResource = new ConfigResource(ConfigResource.Type.TOPIC, TOPIC_NAME);
        Config config = new Config(Collections.singletonList(new ConfigEntry(&quot;preallocate&quot;, &quot;true&quot;)));
        configMap.put(configResource, config);
        
        AlterConfigsResult alterConfigsResult = adminClient.alterConfigs(configMap);
}
</code></pre>
<h3 id="增加-partition-数量">增加 Partition 数量</h3>
<pre><code class="language-java">public static void incrPartitions(int numPartitions) {
        AdminClient adminClient = getAdminClient();
        Map&lt;String, NewPartitions&gt; partitionsMap = new HashMap&lt;&gt;();
        NewPartitions newPartitions = NewPartitions.increaseTo(numPartitions);
        partitionsMap.put(TOPIC_NAME, newPartitions);
        CreatePartitionsResult partitions = adminClient.createPartitions(partitionsMap);
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Kafka（一） 入门]]></title>
        <id>https://flipped94.github.io/post/zawpATTcu/</id>
        <link href="https://flipped94.github.io/post/zawpATTcu/">
        </link>
        <updated>2021-01-23T15:22:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="11-引言">1.1 引言</h2>
<h3 id="什么是事件流">什么是事件流？</h3>
<p>从技术上讲，事件流是指以流的形式实时捕获事件源(如数据库、传感器、移动设备、云服务和软件应用程序)中的数据；持久地存储这些事件流以供以后检索;实时和回顾性地操作、处理和响应事件流；并根据需要将事件流路由到不同的目标的技术。因此，事件流确保了数据的连续流和解释，以便正确的信息在正确的时间出现在正确的位置。</p>
<h3 id="使用事件流可以做什么">使用事件流可以做什么?</h3>
<p>事件流应用于跨越众多行业和组织，包括:</p>
<ul>
<li>实时处理支付和金融交易，如在证券交易所、银行和保险公司。</li>
<li>实时跟踪和监控汽车、卡车、车队和发货，如物流和汽车行业。</li>
<li>持续从物联网设备或其他设备(如工厂和风电场)获取和分析传感器数据。</li>
<li>收集并立即响应客户的互动和订单，例如在零售，酒店和旅游行业以及移动应用程序中。</li>
<li>监测住院病人的情况，预测病情变化，确保在紧急情况下及时治疗。</li>
<li>连接、存储和提供由公司不同部门产生的数据。</li>
<li>作为数据平台、事件驱动架构和微服务的基础。</li>
</ul>
<h3 id="apache-kafka-是事件流平台意味着什么">Apache Kafka 是事件流平台意味着什么？</h3>
<p>Kafka结合了三个关键功能，所以可以用一个经过实战测试的解决方案来实现你的端到端事件流用例:</p>
<ul>
<li>发布（写入）和订阅（读取）事件流，包括从其他系统连续导入/导出数据。</li>
<li>根据需要持久而可靠地存储事件流。</li>
<li>处理事件流的发生或追溯。<br>
所有这些功能都以分布式，高度可扩展，弹性，容错和安全的方式提供。可以选择将 Kafka 部署在裸机硬件，虚拟机和容器，本地以及云中。</li>
</ul>
<h3 id="kafka-概述">Kafka 概述</h3>
<p>Kafka是一个分布式系统，由服务器（Server）和客户端（Client）组成，通过高性能的TCP网络协议进行通信。它可以部署在裸机、虚拟机、内部环境和云环境中的容器上。</p>
<p><strong>服务器</strong>：Kafka 是作为一个集群运行的一个或多个服务器，可以跨多个数据中心或云区域。这些服务器中的一些形成了存储层，称为代理（Broker）。其他服务器运行 Kafka 连接 （ Kafka Connect）以事件流的形式不断导入和导出数据，将 Kafka 与现有系统集成，如关系数据库和其他Kafka集群。Kafka 集群是高度可扩展和容错的，如果它的任何一个服务器故障，其他服务器将接管它的工作，以确保不丢失任何数据的连续操作。<br>
<strong>客户端</strong>：客户端实现编写分布式应用程序和微服务，即使在网络问题或机器故障的情况下，它们也可以并行地、大规模地、以容错的方式读取、写入和处理事件流。Kafka 社区提供了数十个客户端：Java 和 Scala 客户端，更高级别的 Kafka Streams 库，Go、Python、C/C++ 和许多其他编程语言以及REST API。</p>
<h3 id="主要概念及术语">主要概念及术语</h3>
<p>事件记录了在这个世界或你的业务中“发生了一些事情”的事实。它在文档中也称为记录或消息。当你向Kafka读写数据时，你是以事件的形式来完成的。从概念上讲，事件具有键、值、时间戳和可选的元数据头。下面是一个例子:</p>
<p>活动重点:“爱丽丝”<br>
事件值:“向Bob支付了200美元”<br>
事件时间戳:“2020年6月25日下午2:06”<br>
生产者是那些向Kafka发布(写)事件的客户端应用程序，消费者是那些订阅(读和处理)这些事件的客户端应用程序。在Kafka中，生产者和消费者是完全解耦的，并且彼此是不可知的，这是一个关键的设计元素，以实现Kafka众所周知的高可伸缩性。例如，生产者永远不需要等待消费者。Kafka提供了各种各样的保证，比如精确地处理一次事件的能力。</p>
<p>事件被组织并持久地存储在主题中。非常简单，主题类似于文件系统中的文件夹，事件是该文件夹中的文件。一个示例主题名称可以是“payments”。Kafka中的主题总是多生产者和多订阅者:一个主题可以有0个、1个或多个生产者向它写入事件，也可以有0个、1个或多个消费者订阅这些事件。主题中的事件可以根据需要经常读取—与传统消息传递系统不同，事件在使用后不会被删除。相反，你可以通过每个主题的配置设置来定义Kafka应该保留你的事件多长时间，超过这个时间旧的事件就会被丢弃。Kafka的性能与数据大小是一致的，所以长时间存储数据是非常好的。</p>
<p>主题是分区的，这意味着一个主题被分散在位于不同Kafka broker上的许多“桶”上。数据的分布式放置对于可伸缩性非常重要，因为它允许客户机应用程序同时从多个代理读取和写入数据。当一个新事件被发布到一个主题时，它实际上被附加到该主题的一个分区中。具有相同事件键的事件(例如，一个客户或车辆ID)被写入到同一个分区，Kafka保证任何给定topic-partition的消费者总是会按照写入的顺序读取该分区的事件。</p>
<h3 id="主要概念及术语-2">主要概念及术语</h3>
<p>事件（Event）记录了在这个世界或你的业务中“发生了一些事情”的事实。它在文档中也称为记录或消息。向 Kafka 读写数据时，是以事件的形式完成的。从概念上讲，事件具有键（key）、值（value）、时间戳和可选的元数据头。下面是一个例子:</p>
<ul>
<li>事件 Key:“爱丽丝”</li>
<li>事件 Value:“向Bob支付了200美元”</li>
<li>事件时间戳:“2020年6月25日下午2:06”</li>
</ul>
<p>生产者（Producer）是那些向Kafka发布(写)事件的客户端应用程序，消费者（Consumer）是那些订阅(读和处理)这些事件的客户端应用程序。在Kafka中，生产者和消费者是完全解耦的，并且彼此是不可知的，这是实现 Kafka 众所周知的高可伸缩性的一个关键设计。例如，生产者永远不需要等待消费者。Kafka 提供了各种各样的保证，比如精确地处理一次事件的能力。</p>
<p>事件被组织并持久地存储在主题（Topic）中。主题类似于文件系统中的文件夹，事件是该文件夹中的文件。示例的主题名称可以是“payments”。Kafka中的主题总是多生产者和多订阅者：一个主题可以有0个、1个或多个生产者向它写入事件，也可以有0个、1个或多个消费者订阅这些事件。主题中的事件可以根据需要经常读取，与传统消息传递系统不同，事件在使用后不会被删除。相反，可以通过每个主题的配置设置定义事件保留多长时间，超过这个时间旧的事件就会被丢弃。Kafka的性能相对于数据大小实际上是恒定的，因此长时间存储数据是完全可以的。</p>
<p>主题是分区的（Partition），这意味着一个主题被分散在位于不同 Kafka 代理上的许多“桶(Bucket)”上。数据的分布式存储对于可伸缩性非常重要，因为它允许客户端应用程序同时从多个代理读取和写入数据。当一个新事件被发布到一个主题时，它实际上被附加到该主题的一个分区中。具有相同事件 key 的事件（例如，一个客户或车辆ID）将被写入到同一个分区，并且Kafka保证，何给定topic-partition的消费者总是会按照写入的顺序读取该分区的事件。<br>
<img src="https://img.flipped.work/img/20210124001127.png"/><br>
如上图所示，示例主题有四个分区P1-P4。两个不同的生产者客户端通过网络将事件写入主题的分区，彼此独立地向主题发布新事件。具有相同键的事件（在图中用它们的颜色表示）被写入到相同的分区。注意，如果合适的话，两个生产者都可以写同一个分区。</p>
<p>为了使数据具有容错性和高可用性，可以在每个地理区域或数据中心之间复制每个主题，以便始终有多个代理具有数据副本，以防万一出错。常见的生产设置复制因子为3，即数据将始终有三个副本。 此复制在主题分区级别执行。</p>
<p>该入门手册应该足够介绍。 如果您有兴趣，文档的“设计”部分将详细介绍Kafka的各种概念。</p>
<p>让你的数据容错和可用性,每一个主题可以被复制,甚至跨geo-regions或数据中心,这样总有多个经纪人有一份数据以防出错,你想做代理维护,等等。一个常见的生产设置是3的复制因子，也就是说，您的数据总是有三个副本。该复制在主题分区级别上执行。</p>
<h3 id="kafka-api">Kafka API</h3>
<p>除了用于管理和管理任务的命令行工具外，Kafka 还有用于 Java和Scala的五个核心API：</p>
<ul>
<li><a href="https://kafka.apache.org/documentation.html#adminapi">Admin API</a>，用于管理和检查主题，代理和其他Kafka对象。</li>
<li><a href="https://kafka.apache.org/documentation.html#producerapi">Producer API</a>，用于将事件流发布（写）到一个或多个Kafka主题。</li>
<li><a href="https://kafka.apache.org/documentation.html#consumerapi">Consumer API</a>，订阅（读）一个或多个主题并处理为其产生的事件流。</li>
<li><a href="https://kafka.apache.org/documentation/streams">Streams API</a>，实现流处理应用程序和微服务。它提供了处理事件流的高级函数，包括转换、有状态操作（如聚合和连接、开窗、基于事件时间的处理等等）。从一个或多个主题读取输入，以便生成到一个或多个主题的输出，从而有效地将输入流转换为输出流。</li>
<li><a href="https://kafka.apache.org/documentation.html#connect">Connect API</a>，用于构建和运行可重用的数据导入/导出连接器，用于消费（读）或产生（写）来自外部系统和应用程序的事件流，以便与 Kafka 集成。例如，连接到关系数据库（如PostgreSQL）的连接器可能会捕获对一组表的每个更改。通常不需要实现自己的连接器，因为 Kafka 社区已经提供了数百个现成的连接器。</li>
</ul>
<h2 id="12-用例">1.2 用例</h2>
<h3 id="消息队列">消息队列</h3>
<p>Kafka可以很好地替代传消息代理。消息代理的使用有多种原因（将处理与数据生产者分离，缓冲未处理的消息等）。与大多数消息系统相比，Kafka 具有更好的吞吐量、内置分区、复制和容错，这使得它成为大规模消息处理应用程序的良好解决方案。<br>
在这个领域，Kafka 可以与 ActiveMQ 或 RabbitMQ 等传统消息系统相媲美。</p>
<h3 id="网站活跃追踪">网站活跃追踪</h3>
<p>Kafka 最初的用例是能够重建一个用户活跃跟踪管道作为一组实时发布-订阅管道。这意味着站点活跃（页面浏览、搜索或其他用户可能采取的行动）被发布到每个活动类型有一个主题的中心主题。这些管道可用于订阅一系列用例，包括实时处理、实时监控，并加载到Hadoop或离线数据仓库系统中以进行离线处理和报告。<br>
活跃跟踪的工作量通常非常大，因为会为每个用户页面视图生成许多活跃消息。</p>
<h3 id="度量">度量</h3>
<p>Kafka 经常用于运行监控数据。这涉及到从分布式应用程序聚合统计信息来生成集中的操作数据提要。<br>
###日志聚合<br>
许多人使用 Kafka 作为日志聚合解决方案的替代品。日志聚合通常从服务器收集物理日志文件，并将它们放在一个中心位置（可能是文件服务器或HDFS）进行处理。Kafka抽象了文件的细节，并将日志或事件数据作为消息流进行了更清晰的抽象。这允许更低的延迟处理，更容易支持多个数据源和分布式数据消费。与以日志为中心的系统（如Scribe或Flume）相比，Kafka提供了同样好的性能，由于复制而提供了更强的持久性保证，而且端到端延迟要低得多。</p>
<h3 id="流处理">流处理</h3>
<p>许多 Kafka 用户在处理管道中处理数据，该管道由多个阶段组成，原始输入数据从 Kafka 主题消费，然后聚合，丰富，或转换成新的主题，以进一步消费或后续处理。例如，推荐新闻文章的处理管道可能会从 RSS 订阅中抓取文章内容，并将其发布到“articles”主题；进一步的处理可能会对这些内容进行规范化或重复数据删除，并将清洗后的文章内容发布到新主题中；最后一个处理阶段可能会尝试向用户推荐这些内容。这种处理管道根据各个主题创建实时数据流图。从0.10.0.0开始，一个轻量级但强大的流处理库 Kafka Streams 在 Apache Kafka 中可用来执行如上所述的数据处理。除了 Kafka，其他开源流处理工具包括 Apache Storm 和 Apache Samza。</p>
<h2 id="13-快速开始">1.3 快速开始</h2>
<ol>
<li>获取 Kafka<br>
<a href="https://www.apache.org/dyn/closer.cgi?path=/kafka/2.7.0/kafka_2.13-2.7.0.tgz">下载</a>最新的Kafka版本并解压缩：</li>
</ol>
<pre><code class="language-sh">$ tar -xzf kafka_2.13-2.7.0.tgz
$ cd kafka_2.13-2.7.0
</code></pre>
<ol start="2">
<li>启动 KAFKA 环境<br>
<strong>注意：本地环境必须安装了Java 8+</strong>。顺序运行以下命令启动所有服务：</li>
</ol>
<pre><code class="language-sh"># S启动 ZooKeeper 服务
# 注意：很快 Apache Kafka 就将不再需要 Zookeeper.
$ bin/zookeeper-server-start.sh config/zookeeper.properties
</code></pre>
<p>打开另一个终端，执行下面的命令</p>
<pre><code class="language-sh"># 启动 Kafka 服务
$ bin/kafka-server-start.sh config/server.properties
</code></pre>
<p>现在，一个基本的 Kafka 环境运行成功，可以使用了。</p>
<ol start="3">
<li>创建主题存储事件<br>
Kafka 是一个分布式事件流平台，允许在多台机器上读取、写入、存储和处理事件（在文档中也称为记录或消息）。<br>
例如，支付交易、来自移动电话的地理位置更新、发货订单、来自物联网设备或医疗设备的传感器测量等等。这些事件被组织并存储在主题中。主题类似于文件系统中的文件夹，事件是该文件夹中的文件。<br>
因此，编写事件之前，必须创建主题。打开另一个终端并运行:</li>
</ol>
<pre><code class="language-sh">$ bin/kafka-topics.sh --create --topic quickstart-events --bootstrap-server localhost:9092 --partitions 3 --replication-factor 1
</code></pre>
<p>所有 Kafka 命令行工具都有额外的选项：运行不带任何参数的 Kafka-topics.sh 命令来显示使用信息。例如，显示新主题的分区数等细节:</p>
<pre><code>$ bin/kafka-topics.sh --describe --topic quickstart-events --bootstrap-server localhost:9092
</code></pre>
<img src="https://img.flipped.work/img/20210124093047.png"/>
<ol start="4">
<li>向主题写入事件<br>
Kafka 客户端通过网络与 Kafka 代理通信，进行写(或读)事件。一旦收到，代理将以持久和容错的方式存储事件。<br>
运行控制台生产者客户端，在主题中写入事件。默认情况下，输入的每一行将导致一个单独的事件被写入主题。</li>
</ol>
<pre><code class="language-sh">$ bin/kafka-console-producer.sh --topic quickstart-events --broker-list localhost:9092
This is my first event
This is my second event
</code></pre>
<p>可以随时使用 Ctrl-C 停止生产者客户端。</p>
<ol start="5">
<li>读事件<br>
打开另一个终端并运行控制台消费者客户端读取刚刚创建的事件:</li>
</ol>
<pre><code class="language-sh">$ bin/kafka-console-consumer.sh --topic quickstart-events --from-beginning --bootstrap-server localhost:9092
This is my first event
This is my second event
</code></pre>
<p>同样，可以随时使用 Ctrl-C 停止消费者客户端。</p>
<ol start="6">
<li>
<p>使用 KAFKA CONNECT 导入/导出数据作为事件流<br>
可能在关系数据库或传统消息系统等现有系统中拥有大量数据，以及许多已经使用这些系统的应用程序。Kafka Connect 允许不断地从外部系统摄取数据到 Kafka，反之亦然。因此，将现有的系统与Kafka集成是非常容易的。为了使这个过程更容易，Kafka 社区提供了很多连接器。<br>
查看 <a href="https://kafka.apache.org/documentation/#connect">Kafka Connect 部分</a>，了解更多关于如何导入/导出数据到/出 Kafka。</p>
</li>
<li>
<p>用 KAFKA 流处理事件<br>
一旦数据作为事件存储在 Kafka 中，就可以使用 Kafka Streams 客户端库处理数据。它允许实现关键任务实时应用程序和微服务，其中输入和/或输出数据存储在Kafka主题中。Kafka Streams 结合了在客户端编写和部署标准 Java 和 Scala 应用程序的简便性以及 Kafka 服务器端集群技术的优势，使这些应用程序具有高度可伸缩性，弹性，容错性和分布式性。 该库支持一次精确处理，有状态操作和聚合，开窗，联接，基于事件时间的处理等等。</p>
</li>
</ol>
<p>下面是如何实现流行的WordCount算法:</p>
<pre><code class="language-java">KStream&lt;String, String&gt; textLines = builder.stream(&quot;quickstart-events&quot;);

KTable&lt;String, Long&gt; wordCounts = textLines
            .flatMapValues(line -&gt; Arrays.asList(line.toLowerCase().split(&quot; &quot;)))
            .groupBy((keyIgnored, word) -&gt; word)
            .count();

wordCounts.toStream().to(&quot;output-topic&quot;), Produced.with(Serdes.String(), Serdes.Long()));
</code></pre>
<p><a href="https://kafka.apache.org/25/documentation/streams/quickstart">Kafka Stream Demo</a> 和<a href="https://kafka.apache.org/25/documentation/streams/tutorial">应用程序开发教程</a>演示了如何编写和运行这样的流应用程序。</p>
<ol start="8">
<li>终止 KAFKA</li>
</ol>
<ul>
<li>使用 Ctrl-C 停止生产者和消费者客户端（如果还没有这样做的话）。</li>
<li>用 Ctrl-C 停止 Kafka 代理。</li>
<li>最后，按 Ctrl-C 停止 ZooKeeper 服务器。</li>
</ul>
<p>如果还想删除本地 Kafka 环境的任何数据，包括已创建事件，只需运行命令:</p>
<pre><code class="language-sh">$ rm -rf /tmp/kafka-logs /tmp/zookeeper
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[配置 Elasticsearch]]></title>
        <id>https://flipped94.github.io/post/ng_w0EohH/</id>
        <link href="https://flipped94.github.io/post/ng_w0EohH/">
        </link>
        <updated>2021-01-23T01:47:43.000Z</updated>
        <content type="html"><![CDATA[<p>Elasticsearch 有一些默认设置，只需要少量的配置。可以使用<a href="https://www.elastic.co/guide/en/elasticsearch/reference/7.10/cluster-update-settings.html">集群更新设置 API </a>在正在运行的集群上更改大多数设置。</p>
<p>配置文件应包含特定节点的设置（例如 <code>node.name</code> 和路径），或者节点为了能够加入集群而需要的设置，例如 <code>cluster.name</code> 和 <code>network.host</code>。</p>
<h2 id="配置文件">配置文件</h2>
<p>Elasticsearch 有三个配置文件：</p>
<ul>
<li><code>elasticsearch.yml</code> 用于配置 Elasticsearch</li>
<li><code>jvm.options</code> 用于配置 Elasticsearch JVM</li>
<li><code>log4j2.properties</code> 用于配置 Elasticsearch 日志记录<br>
这些文件位于 <code>config</code> 目录中，其默认位置取决于安装是归档发行版（tar.gz或zip）还是软件包发行版（Debian或RPM软件包）。</li>
</ul>
<p>对于归档发行版，配置目录位置默认为 <code>$ES_HOME/config</code>。可以通过 <code>ES_PATH_CONF</code> 环境变量更改 <code>config</code> 目录的位置，如下所示：</p>
<pre><code class="language-sh">ES_PATH_CONF=/path/to/my/config ./bin/elasticsearch
</code></pre>
<p>或者，通过命令行或 Shell 配置文件导出 ES_PATH_CONF 环境变量。</p>
<p>对于软件包发行版，<code>config</code> 目录位置默认为 <code>/etc/elasticsearch</code>。也可以通过 <code>ES_PATH_CONF</code> 环境变量更改 <code>config</code> 目录的位置，需要注意的是，在 shell 中进行设置是不够的。相反，此变量来自 <code>/etc/default/elasticsearch</code>（对于 Debian 软件包）和 <code>/etc/sysconfig/elasticsearch</code>（对于RPM软件包）。 需要相应地在这些文件之一中编辑 <code>ES_PATH_CONF=/etc/elasticsearch</code> 条目更改配置目录位置。</p>
<h3 id="配置文件格式">配置文件格式</h3>
<p>配置格式为 <code>YAML</code>。设置数据和日志目录的路径的示例：</p>
<pre><code class="language-yaml">path:
    data: /var/lib/elasticsearch
    logs: /var/log/elasticsearch
</code></pre>
<p>设置也可以是下面的方式：</p>
<pre><code class="language-yaml">path.data: /var/lib/elasticsearch
path.logs: /var/log/elasticsearch
</code></pre>
<p>在YAML中，可以将非标量值格式化为序列：</p>
<pre><code class="language-yaml">discovery.seed_hosts:
   - 192.168.1.10:9300
   - 192.168.1.11
   - seeds.mydomain.com
</code></pre>
<p>尽管不太常见，也可以将非标量值格式化为数组：</p>
<pre><code class="language-yaml">discovery.seed_hosts: [&quot;192.168.1.10:9300&quot;, &quot;192.168.1.11&quot;, &quot;seeds.mydomain.com&quot;]
</code></pre>
<h3 id="环境变量">环境变量</h3>
<p>配置文件中用 <code>$ {...}</code> 标记引用的环境变量将替换为环境变量的值。 例如：</p>
<pre><code class="language-yaml">node.name:    ${HOSTNAME}
network.host: ${ES_NETWORK_HOST}
</code></pre>
<p>环境变量的值必须是简单的字符串。使用逗号分隔的字符串 Elasticsearch 将解析为列表。例如，Elasticsearch 将以下字符串拆分为 <code>${HOSTNAME}</code> 环境变量的列表：</p>
<pre><code class="language-sh">export HOSTNAME=“host1,host2&quot;
</code></pre>
<h3 id="集群和节点设置类型">集群和节点设置类型</h3>
<p>可以根据配置方式对集群和节点设置进行分类：</p>
<h4 id="动态设置">动态设置</h4>
<p>使用集群更新设置 API 在正在运行的集群上配置和更新动态设置。还可以使用 <code>elasticsearch.yml</code> 在未启动或关闭的节点上本地配置动态设置。</p>
<p>使用集群更新设置 API 的更新可以是永久设置，适用于集群重新启动。也可以是临时设置，在集群重新启动后重置。还可以使用API为临时或永久设置赋 <code>null</code> 值重置它们。</p>
<p>如果使用多种方法配置相同的设置，Elasticsearch 将按以下优先级应用设置：</p>
<ul>
<li>临时设置</li>
<li>永久设置</li>
<li><code>elasticsearch.yml</code> 设置</li>
<li>默认值</li>
</ul>
<p>根据以上规则，可以用临时设置覆盖永久设置或 <code>elasticsearch.yml</code> 设置。但是，<code>elasticsearch.yml</code> 的改变不会覆盖已定义的临时或永久设置。</p>
<p><em>最好使用群集更新设置API设置动态的，群集范围内的设置，并且仅对本地配置使用elasticsearch.yml。 使用群集更新设置API可确保所有节点上的设置都相同。 如果您不小心在不同节点上的elasticsearch.yml中配置了不同的设置，则可能很难发现差异。</em></p>
<h4 id="静态设置">静态设置</h4>
<p>只能使用 <code>elasticsearch.yml</code> 在未启动或关闭的节点上配置静态设置。<br>
必须在集群中的每个相关节点上设置静态设置。</p>
<h2 id="jvm-配置">JVM 配置</h2>
<p>几乎不需要更改 Java 虚拟机（JVM）配置。最可能的更改是设置堆大小。使用 <code>jvm.options</code> 文件或 <code>ES_JAVA_OPTS</code> 环境变量设置。</p>
<p>设置或覆盖 JVM 配置的首选方法是 <code>jvm.options</code> 文件。从 tar 或 zip 发行版安装时，根 <code>jvm.options</code> 配置文件是 <code>config/jvm.options</code>，并且可以将自定义 JVM 配置文件添加到 <code>config/jvm.options.d/</code> 目录下。从 Debian 或 RPM 软件包安装时，根 <code>jvm.options</code> 配置文件是 <code>/etc/elasticsearch/jvm.options</code>，并且可以将自定义 JVM 配置文件添加到 <code>/etc/elasticsearch/jvm.options.d/</code> 目录下。使用 Elasticsearch 的 Docker 发行版时，可以将自定义 JVM 配置文件绑定到 <code>/usr/share/elasticsearch/config/jvm.options.d/</code> 中。最好的做法是不要修改根 <code>jvm.options</code> 文件，使用自定义 JVM 配置文件。自定义 JVM 配置按字典顺序处理。</p>
<p>JVM 配置文件必须具有后缀 <code>.options</code>，并包含使用特殊语法的以行分隔的JVM参数列表：</p>
<ul>
<li>仅由空格组成的行将被忽略</li>
<li>以 <code>＃</code> 开头的行被视为注释并被忽略</li>
</ul>
<pre><code class="language-options"># this is a comment
</code></pre>
<ul>
<li>以 <code>-</code> 开头的行，独立于 JVM 版本</li>
</ul>
<pre><code class="language-options">-Xmx2g
</code></pre>
<ul>
<li>以数字开头，跟着是 <code>:</code>，然后是 <code>-</code> 的行，仅在 JVM 版本与数字匹配时才适用。</li>
</ul>
<pre><code class="language-options">8:-Xmx2g
</code></pre>
<ul>
<li>以数字开头，跟着是 <code>-</code>，然后是 <code>:</code> 的行，仅在 JVM 版本大于或等于该数字时才适用</li>
</ul>
<pre><code class="language-options">8-:-Xmx2g
</code></pre>
<ul>
<li>以数字开头，跟着是 <code>-</code>，然后是数字的行，仅在 JVM 版本在两个数字范围内时才适用。</li>
</ul>
<pre><code class="language-options">8-9:-Xmx2g
</code></pre>
<ul>
<li>其他所有行均被拒绝</li>
</ul>
<p>另一种机制是通过 <code>ES_JAVA_OPTS</code> 环境变量。例如：</p>
<pre><code class="language-sh">export ES_JAVA_OPTS=&quot;$ES_JAVA_OPTS -Djava.io.tmpdir=/path/to/temp/dir&quot;
./bin/elasticsearch
</code></pre>
<p>使用 RPM 或 Debian 软件包时，可以在系统配置文件中指定 <code>ES_JAVA_OPTS</code>。</p>
<p>JVM 具有用于观察 <code>JAVA_TOOL_OPTIONS</code> 环境变量的内置机制。Elasticsearch 有意在打包脚本中忽略此环境变量，这样做的主要原因是，在某些操作系统（例如Ubuntu）上，默认情况下通过此环境变量安装了代理程序，不想干扰 Elasticsearch。</p>
<p>此外，其他一些 Java 程序也支持 <code>JAVA_OPTS</code> 环境变量。这不是 JVM 内置机制，而是生态系统中的约定。但是，Elasticsearch 不支持此环境变量，而是通过 <code>jvm.options</code> 文件或环境变量 <code>ES_JAVA_OPTS</code> 配置 JVM。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Elasticsearch 入门]]></title>
        <id>https://flipped94.github.io/post/NHGF0CR8M/</id>
        <link href="https://flipped94.github.io/post/NHGF0CR8M/">
        </link>
        <updated>2021-01-22T09:50:01.000Z</updated>
        <content type="html"><![CDATA[<p>本文主要介绍 Elasticsearch 的入门使用，了解如何使用 REST API 来存储，搜索和分析数据。内容如下：</p>
<ul>
<li>启动并运行 Elasticsearch 集群</li>
<li>索引示例文件</li>
<li>使用 Elasticsearch 查询语言搜索文档</li>
<li>分析结果</li>
</ul>
<p>环境：WLS2 Ubuntu 20.04，Elasticsearch 7.1.0, Kibana 7.1.0， jdk1.8</p>
<h2 id="运行-elasticsearch">运行 Elasticsearch</h2>
<p>根据以下步骤，在本地运行有三个节点的 Elasticsearch 集群：</p>
<ol>
<li>下载 Elasticsearch</li>
</ol>
<pre><code class="language-sh">wget https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-7.1.0-linux-x86_64.tar.gz
</code></pre>
<ol start="2">
<li>解压</li>
</ol>
<pre><code class="language-sh">tar -xvf  elasticsearch-7.1.0-linux-x86_64.tar.gz
</code></pre>
<ol start="3">
<li>在 <code>bin</code> 目录下启动 Elasticsearch</li>
</ol>
<pre><code class="language-sh">cd elasticsearch-7.10.2/bin
./elasticsearch
</code></pre>
<p>目前为止，就已经启动了有一个节点的 Elasticsearch 集群。</p>
<ol start="4">
<li>启动另外两个 Elasticsearch 实例<br>
启动另外两个节点后，就是典型的多节点集群。需要为每个节点指定唯一的数据和日志路径。</li>
</ol>
<pre><code class="language-sh">./elasticsearch -Epath.data=data2 -Epath.logs=log2
./elasticsearch -Epath.data=data3 -Epath.logs=log3
</code></pre>
<ol start="5">
<li>使用 cat health API 验证三节点集群是否正在运行。 cat API 以比原始 JSON 更易于阅读的格式返回有关集群和索引的信息。</li>
</ol>
<pre><code class="language-sh">curl -X GET localhost:9200/_cat/health?v=true
</code></pre>
<img src="https://img.flipped.work/img/20210122184422.png"/>
后面将使用体验更好的 Kibana （安装过程类似，下载、解压、在 `bin` 目录下运行 `kibana`，在浏览器打开`localhost:5601`）通过 Dev Tools 提交请求。
<h2 id="索引文档">索引文档</h2>
<p>集群启动并运行后，就可以索引数据了。Elasticsearch 有多种方式将 JSON 文档放入Elasticsearch 索引中。<br>
可以通过简单的 PUT 请求直接执行此操作，该请求指定要添加文档的索引，唯一的文档ID，以及请求体中的一个或多个 <code>&quot;field&quot;: &quot;value&quot;</code>对：</p>
<pre><code class="language-js">PUT /customer/_doc/1
{
  &quot;name&quot;: &quot;John Doe&quot;
}
</code></pre>
<p>该请求将自动创建 <code>customer</code> 索引（如果尚不存在），添加ID为1的新文档，存储并索引 <code>name</code> 字段。<br>
由于这是一个新文档，因此响应显示该操作的结果是创建了该文档的版本1：</p>
<pre><code class="language-json">{
  &quot;_index&quot; : &quot;customer&quot;,
  &quot;_type&quot; : &quot;_doc&quot;,
  &quot;_id&quot; : &quot;1&quot;,
  &quot;_version&quot; : 1,
  &quot;result&quot; : &quot;created&quot;,
  &quot;_shards&quot; : {
    &quot;total&quot; : 2,
    &quot;successful&quot; : 2,
    &quot;failed&quot; : 0
  },
  &quot;_seq_no&quot; : 26,
  &quot;_primary_term&quot; : 4
}
</code></pre>
<p>可以从群集中的任何节点立即使用新文档，可以指定文档ID的GET请求检索它：</p>
<pre><code class="language-js">GET /customer/_doc/1
</code></pre>
<p>该响应表明找到了具有指定ID的文档，并显示了被索引的源字段。</p>
<pre><code class="language-json">{
  &quot;_index&quot; : &quot;customer&quot;,
  &quot;_type&quot; : &quot;_doc&quot;,
  &quot;_id&quot; : &quot;1&quot;,
  &quot;_version&quot; : 1,
  &quot;_seq_no&quot; : 26,
  &quot;_primary_term&quot; : 4,
  &quot;found&quot; : true,
  &quot;_source&quot; : {
    &quot;name&quot;: &quot;John Doe&quot;
  }
}
</code></pre>
<h2 id="批量索引文档">批量索引文档</h2>
<p>如果索引的文档很多，则可以使用批量 API 批量提交它们。批量处理文档操作比单独提交请求要快得多，因为它可以最大程度地减少网络往返次数。</p>
<p>最佳批处理大小取决于许多因素：文档大小和复杂性，索引编制和搜索负载以及群集可用的资源。 一个好的批处理包含1,000至5,000个文档，总有效负载在5MB至15MB之间。</p>
<p>将一些数据导入 Elasticsearch，然后搜索和分析：</p>
<ol>
<li><a href="https://img.flipped.work/accounts.json">点击下载 accounts.json</a>  示例数据集。 该数据集中的文档包含下信息的用户帐户：</li>
</ol>
<pre><code class="language-json">{
  &quot;account_number&quot;: 0,
  &quot;balance&quot;: 16623,
  &quot;firstname&quot;: &quot;Bradshaw&quot;,
  &quot;lastname&quot;: &quot;Mckenzie&quot;,
  &quot;age&quot;: 29,
  &quot;gender&quot;: &quot;F&quot;,
  &quot;address&quot;: &quot;244 Columbus Place&quot;,
  &quot;employer&quot;: &quot;Euron&quot;,
  &quot;email&quot;: &quot;bradshawmckenzie@euron.com&quot;,
  &quot;city&quot;: &quot;Hobucken&quot;,
  &quot;state&quot;: &quot;CO&quot;
}
</code></pre>
<ol start="2">
<li>使用如下 <code>_bulk</code> 请求将帐户数据索引到 <code>banl</code> 索引中：</li>
</ol>
<pre><code class="language-sh">curl -H &quot;Content-Type: application/json&quot; -XPOST &quot;localhost:9200/bank/_bulk?pretty&amp;refresh&quot; --data-binary &quot;@accounts.json&quot;
</code></pre>
<ol start="3">
<li>使用下面的请求验证结果</li>
</ol>
<pre><code class="language-sh">curl &quot;localhost:9200/_cat/indices?v=true&quot;
</code></pre>
<p>响应表明成功索引了1,000个文档。<br>
<img src="https://img.flipped.work/img/20210122221256.png"/></p>
<h2 id="搜索数据">搜索数据</h2>
<p>Elasticsearch 索引后，将请求发送到 <code>_search</code> 端点进行搜索。使用 Elasticsearch Query DSL 在请求体中指定搜索条件访问全部搜索功能。在请求 URI 中指定要搜索的索引的名称。</p>
<p>例如，以下请求将检索 <code>bank</code> 索引中按帐号排序的所有文档：</p>
<pre><code class="language-sh">GET /bank/_search
{
  &quot;query&quot;: { &quot;match_all&quot;: {} },
  &quot;sort&quot;: [
    { &quot;account_number&quot;: &quot;asc&quot; }
  ]
}
</code></pre>
<p>默认情况下，响应的 <code>hits</code> 部分包含与搜索条件匹配的前10个文档：</p>
<pre><code class="language-json">{
    &quot;took&quot;: 68,
    &quot;timed_out&quot;: false,
    &quot;_shards&quot;: {
        &quot;total&quot;: 1,
        &quot;successful&quot;: 1,
        &quot;skipped&quot;: 0,
        &quot;failed&quot;: 0
    },
    &quot;hits&quot;: {
        &quot;total&quot;: {
            &quot;value&quot;: 1000,
            &quot;relation&quot;: &quot;eq&quot;
        },
        &quot;max_score&quot;: null,
        &quot;hits&quot;: [
            {
                &quot;_index&quot;: &quot;bank&quot;,
                &quot;_type&quot;: &quot;_doc&quot;,
                &quot;_id&quot;: &quot;0&quot;,
                &quot;_score&quot;: null,
                &quot;_source&quot;: {
                    &quot;account_number&quot;: 0,
                    &quot;balance&quot;: 16623,
                    &quot;firstname&quot;: &quot;Bradshaw&quot;,
                    &quot;lastname&quot;: &quot;Mckenzie&quot;,
                    &quot;age&quot;: 29,
                    &quot;gender&quot;: &quot;F&quot;,
                    &quot;address&quot;: &quot;244 Columbus Place&quot;,
                    &quot;employer&quot;: &quot;Euron&quot;,
                    &quot;email&quot;: &quot;bradshawmckenzie@euron.com&quot;,
                    &quot;city&quot;: &quot;Hobucken&quot;,
                    &quot;state&quot;: &quot;CO&quot;
                },
                &quot;sort&quot;: [
                    0
                ]
            }
            还有9个文档
        ]
    }
}
</code></pre>
<p>该响应还提供有关搜索请求的以下信息：</p>
<ul>
<li>took：Elasticsearch 运行查询所花费的时间（以毫秒为单位）</li>
<li>timed_out：搜索请求是否超时</li>
<li>_shards：搜索了多少个分片，以及成功，失败或跳过了多少个分片。</li>
<li>max_score：找到的最相关文档的分数</li>
<li>hits.total.value：找到了多少个匹配的文档</li>
<li>hits.sort：文档的排序位置（不按相关性得分排序时）</li>
<li>hits._score：文档的相关性得分（使用match_all时不适用）</li>
</ul>
<p>每个搜索请求都是独立的：Elasticsearch 不会在请求中维护任何状态信息。在请求中指定 <code>from</code> 和 <code>size</code> 参数实现分页搜索。例如，以下请求的命中10到19：</p>
<pre><code class="language-sh">GET /bank/_search
{
  &quot;query&quot;: { &quot;match_all&quot;: {} },
  &quot;sort&quot;: [
    { &quot;account_number&quot;: &quot;asc&quot; }
  ],
  &quot;from&quot;: 10,
  &quot;size&quot;: 10
}
</code></pre>
<p>下面开始构建比 <code>match_all</code> 复杂的查询。</p>
<p>使用 <code>match</code> 进行多词查询。例如，以下请求搜索 <code>address</code> 字段，查找其地址包含 <code>mill</code> 或者 <code>lane</code> 的客户：</p>
<pre><code class="language-sh">GET /bank/_search
{
  &quot;query&quot;: { &quot;match&quot;: { &quot;address&quot;: &quot;mill lane&quot; } }
}
</code></pre>
<p>使用 <code>match_phrase</code> 而不是 <code>match</code>，执行短语搜索而不是匹配单词。例如，以下请求仅匹配包含短语 <code>mill lane</code> 的地址：</p>
<pre><code class="language-sh">GET /bank/_search
{
  &quot;query&quot;: { &quot;match_phrase&quot;: { &quot;address&quot;: &quot;mill lane&quot; } }
}
</code></pre>
<p>可以使用 <code>bool</code> 查询组合多个查询条件构造更复杂的查询。根据需要（必须匹配），期望（应该匹配）或不期望（必须不匹配）指定条件。</p>
<p>例如，以下请求在银行索引中搜索年龄40岁，但不包括居住在爱达荷州（ID）的帐户：</p>
<pre><code class="language-sh">GET /bank/_search
{
  &quot;query&quot;: {
    &quot;bool&quot;: {
      &quot;must&quot;: [
        { &quot;match&quot;: { &quot;age&quot;: &quot;40&quot; } }
      ],
      &quot;must_not&quot;: [
        { &quot;match&quot;: { &quot;state&quot;: &quot;ID&quot; } }
      ]
    }
  }
}
</code></pre>
<p><code>bool</code> 查询中的每个 <code>must</code>，<code>should</code> 和 <code>must_not</code> 元素都称为查询子句。文档满足每个 <code>must</code> 或 <code>should</code> 子句中的条件有助于提高文档的相关性得分。分数越高，文档就越符合搜索条件。默认情况下，Elasticsearch 相关性分数排名返回文档。</p>
<p><code>must_not</code> 子句中的条件被视为<strong>过滤器（filter）</strong>。它影响文件是否包含在结果中，但不会影响文件的评分方式。可以显式指定任意过滤器，以基于结构化数据包括或排除文档。</p>
<p>例如，以下请求使用范围过滤器将结果限制为余额在20,000美元到30,000美元（含）之间的帐户。</p>
<pre><code class="language-sh">GET /bank/_search
{
  &quot;query&quot;: {
    &quot;bool&quot;: {
      &quot;must&quot;: { &quot;match_all&quot;: {} },
      &quot;filter&quot;: {
        &quot;range&quot;: {
          &quot;balance&quot;: {
            &quot;gte&quot;: 20000,
            &quot;lte&quot;: 30000
          }
        }
      }
    }
  }
}
</code></pre>
<h2 id="使用聚合分析结果">使用聚合分析结果</h2>
<p>Elasticsearch 聚合能够获取有关搜索结果的元信息，并回答诸如“德克萨斯州有多少个帐户持有人？”之类的问题。 或“田纳西州的平均余额是多少？” 可以在一个请求中搜索文档，过滤命中并使用聚合分析结果。</p>
<p>例如，以下请求使用 <code>terms</code> 聚合将 <code>bank</code> 索引中的所有帐户按状态分组，并以降序返回帐户数量最多的十个州：</p>
<pre><code class="language-sh">GET /bank/_search
{
  &quot;size&quot;: 0,
  &quot;aggs&quot;: {
    &quot;group_by_state&quot;: {
      &quot;terms&quot;: {
        &quot;field&quot;: &quot;state.keyword&quot;
      }
    }
  }
}
</code></pre>
<p>响应中的 <code>buckets</code> 是 <code>state</code> 字段的值。<code>doc_count</code> 显示每个州下的帐户数。 例如，ID（爱达荷州）中有27个帐户。因为请求中的 <code>size=0</code>，所以响应仅包含聚合结果。</p>
<pre><code class="language-json">{
  &quot;took&quot; : 65,
  &quot;timed_out&quot; : false,
  &quot;_shards&quot; : {
    &quot;total&quot; : 1,
    &quot;successful&quot; : 1,
    &quot;skipped&quot; : 0,
    &quot;failed&quot; : 0
  },
  &quot;hits&quot; : {
    &quot;total&quot; : {
      &quot;value&quot; : 1000,
      &quot;relation&quot; : &quot;eq&quot;
    },
    &quot;max_score&quot; : null,
    &quot;hits&quot; : [ ]
  },
  &quot;aggregations&quot; : {
    &quot;group_by_state&quot; : {
      &quot;doc_count_error_upper_bound&quot; : 0,
      &quot;sum_other_doc_count&quot; : 743,
      &quot;buckets&quot; : [
        {
          &quot;key&quot; : &quot;TX&quot;,
          &quot;doc_count&quot; : 30
        },
        {
          &quot;key&quot; : &quot;MD&quot;,
          &quot;doc_count&quot; : 28
        },
        {
          &quot;key&quot; : &quot;ID&quot;,
          &quot;doc_count&quot; : 27
        },
        {
          &quot;key&quot; : &quot;AL&quot;,
          &quot;doc_count&quot; : 25
        },
        {
          &quot;key&quot; : &quot;ME&quot;,
          &quot;doc_count&quot; : 25
        },
        {
          &quot;key&quot; : &quot;TN&quot;,
          &quot;doc_count&quot; : 25
        },
        {
          &quot;key&quot; : &quot;WY&quot;,
          &quot;doc_count&quot; : 25
        },
        {
          &quot;key&quot; : &quot;DC&quot;,
          &quot;doc_count&quot; : 24
        },
        {
          &quot;key&quot; : &quot;MA&quot;,
          &quot;doc_count&quot; : 24
        },
        {
          &quot;key&quot; : &quot;ND&quot;,
          &quot;doc_count&quot; : 24
        }
      ]
    }
  }
}
</code></pre>
<p>可以组合聚合构建更复杂的数据聚合。例如，以下请求将平均聚合嵌套在 <code>group_by_state</code> 聚合内，计算每个州的平均余额。</p>
<pre><code class="language-sh">GET /bank/_search
{
  &quot;size&quot;: 0,
  &quot;aggs&quot;: {
    &quot;group_by_state&quot;: {
      &quot;terms&quot;: {
        &quot;field&quot;: &quot;state.keyword&quot;
      },
      &quot;aggs&quot;: {
        &quot;average_balance&quot;: {
          &quot;avg&quot;: {
            &quot;field&quot;: &quot;balance&quot;
          }
        }
      }
    }
  }
}
</code></pre>
<p>通过在 <code>terms</code> 聚合中指定顺序来使用嵌套聚合的结果进行排序，而不必按计数对结果进行排序：</p>
<pre><code class="language-sh">GET /bank/_search
{
  &quot;size&quot;: 0,
  &quot;aggs&quot;: {
    &quot;group_by_state&quot;: {
      &quot;terms&quot;: {
        &quot;field&quot;: &quot;state.keyword&quot;,
        &quot;order&quot;: {
          &quot;average_balance&quot;: &quot;desc&quot;
        }
      },
      &quot;aggs&quot;: {
        &quot;average_balance&quot;: {
          &quot;avg&quot;: {
            &quot;field&quot;: &quot;balance&quot;
          }
        }
      }
    }
  }
}
</code></pre>
<p>除了像这样的基本聚合，Elasticsearch 还提供了专门的聚合，用于在多个字段上操作并分析特定类型的数据，例如日期，IP地址和地理数据。</p>
]]></content>
    </entry>
</feed>